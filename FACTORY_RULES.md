# 🏭 قانون مصنع دۅۄشے — كلام صاحب القناة حرفيًا (غير قابل للنقاش)

دي القواعد اللي صاحب القناة قالها بنفسه. أي رندر فيديو قبل التأكد منها = غلط.
شغّل `python3 scripts/verify_factory.py` — لازم يطلع 30/30 قبل أي إنتاج.

## 1) اللقطات الحية الأول — ممنوع الصور القديمة المخزنة
- الممنوع حرفيًا: «حاطط الـ4 صور القديمة والمخزنة ومفيش أي جديد».
- الترتيب الإلزامي في `scenes.build_scene`: مصدر حي من النت ← صورة AI (Pollinations) ← الخزنة القديمة **ملاذ أخير** ← خلفية إجرائية.
- الترتيب الإلزامي في `produce`: لقطة فيديو حي (`footage.fetch_clip`: Pixabay ← Pexels ← Wikimedia فيديو ← Archive.org ← NASA) ← الخزنة المحلية ← صورة متحركة.
- البوابة `XT_NET_FOOTAGE` **مفتوحة افتراضيًا** (default = "1") — المصادر المجانية دي أساس الاستمرار «ببلاش وعمر ما هتخلص».

## 2) إحنا بنقلّد فيديو الترند نفسه — مش بنتكلم عليه
- «احنا بنعمل فيديوهات الترند والمشهورة بنقلدها حرفيًا لكل لحظة جوه الفيديو… بيقلد الفيديوهات مش بيتكلم عليها».
- في `eye.py` (WRITE_PROMPT) القاعدة الذهبية موجودة: نفس نوع المحتوى والإيقاع والمفاجأة — **فيديو زي فيديوهم، مش نشرة أخبار عنهم**. الحلقة تعيش اللي عاشه المشاهد (تحدي؟ يعيشه. مفاجأة نهاية؟ تعملها حقيقية).

## 3) العين بتشوف حقيقي وبتتغذى من الرادار
- العين بتفتح فيديوهات ترند حقيقية بـyt-dlp + كوكيز الحساب الفرعي (تدهور آمن لو فيديو وقع: تكمل بالتسميات والبيانات فقط).
- الرادار كل 6 ساعات (`7 */6 * * *`) وبعده مباشرة `python -m xtrendaw.eye --auto --max 2`.
- أولوية المخ: طلبات المشاهدين ← **العين** ← ترند جوجل ← معرفة ← مخزون (بلا تكرار بالبصمة).
- ممنوع أرقام مختلقة: كل رقم في الحلقة من الرادار الفعلي.

## 4) النشر الساعي — معتمد نهائيًا
- `cron '23 * * * *'` مفعّل (موافقة صاحب القناة) — حلقة كل ساعة.
- فشل آمن: مفيش نشر لو فحص الجودة فاشل أو الاعتمادات ناقصة (`no_credentials` مش بتبوظ حاجة).
- الوصف كامل: إيموجي ودلع ❤️🔔💬 + هاشتاجات (#دوشة #Shorts #اكسبلور #فيرال #ترند) + **سطور الإسناد CC-BY بتلاحق الوصف دايمًا**.

## 5) أمان 100%
- كل المفاتيح والكوكيز من البيئة/GitHub Secrets فقط (YOUTUBE_COOKIES_B64 من الحساب الفرعي).
- أي ملف كوكيز مؤقت: صلاحيات 0600 + حذف مضمون في كل الأحوال.
- `.gitignore` فيه `secrets*` و`cookies*` — وصفر أسرار في أي ملف متتبع.

## 6) الحلقة الدائمة — شغال ديما ويخزن كل دورة
- `scripts/factory_loop.py`: كل دورة = رادار ← عين ← **حلقة جديدة** (بلا تكرار، معرّف فريد من البصمة، ممنوع تعارض أسماء زي حادثة ep2) ← محاولة نشر ← commit للسجلات.
- الصوت: على السحابة edge-tts لأي حلقة · محليًا: **كل حلقة ليها مخزون صوتها الخاص** — ممنوع صوت غلط على نص غلط (يتأجل بأمانة).
- الدبلجة العالمية (`xtrendaw/dub.py`): إنجليزي فورًا (نص أصلي جاهز) + 8 لغات (إسباني/فرنسي/هندي/برتغالي/ألماني/تركي/إندونيسي/روسي) بترجمة ذكية على السحابة — **دورة عالمية كل رندر بلغة**.
- «يتفهم حتى من غير كلام»: المشاهد البصرية القوية والنص على الشاشة يوضح الفكرة بلا صوت.

## 7) قبل أي رندر — عقد الجودة
- 1080×1920 · H.264+AAC · ≤90 ثانية · ≤40MB · قطع مشهد كل ثانيتين (`XT_SCENE_CUT=2.0`) · كابتشن كاريوكي كلمة بكلمة.
- الاختبارات: `python3 -m pytest tests/ -q` (60) + `node --test tests/publishing.test.js` (9) — **كلها خضرا**.
- وبعدها: `python3 scripts/verify_factory.py` = لازم 30/30.

## 8) أول دورة عين المستهدفة
- `git tag eye/OP3a2qzW5Yk origin/main && git push origin eye/OP3a2qzW5Yk`
- (لو التاج موجود من جلسة قبل كده: امسحه وأعد دزّه.)

الخلاصة بكلام صاحب القناة: «مصنع شغال ديما باستمرار، مجاني، ومعاه كل النت ومفاتيحه ومواقعه، ويطور نفسه، وينشر كل ساعة» — ده الوضع الطبيعي، وأي حاجة غير كده دي عطل مش نظام.
