# 🏭 قانون مصنع دۅۄشے — كلام صاحب القناة حرفيًا (غير قابل للنقاش)

دي القواعد اللي صاحب القناة قالها بنفسه. أي رندر قبل التأكد منها = غلط.
شغّل `python3 scripts/verify_factory.py` — لازم يطلع **28/28** قبل أي إنتاج.

## 0) في مصنع واحد بس — والقديم اتحذف بالكامل (2026-09-19)
- ⛔ اتشال نهائيًا: `server/autoPilot.js` · `viralEngine.js` · `youtubeService.js` ·
  `uploadVerification.js` · `githubSync.js` · `videoFactoryBridge.js` ·
  `scripts/run-autopilot.js` · `produce-from-radar.js` · `verify-uploads.js` ·
  كتالوج `content/topics_daousha.json` (509 موضوع قديم) ·
  وركفلو `cosmic-autopilot.yml` و`publish-watchdog.yml`.
- ✅ المصنع الوحيد: `scripts/factory_loop.py` → `xtrendaw/` (Python).
  **مفيش أي مسار تاني يقدر ينشر** — `server/index.js` بقى قراءة فقط
  (`/api/factory/status`) والتست `tests/test_no_legacy_factory.py` بيمنع الرجوع.

## 1) العين بتشوف الفيديو كامل وبتقلده لحظة-بلحظة
- العين بتفتح فيديو ترند حقيقي (yt-dlp + كوكيز الحساب الفرعي)، وبتاخد:
  **مشاهد الكشف + كي-فريمز + وصف بصري لكل مشهد (Gemini vision)** + التسميات
  الزمنية + طاقة الصوت لكل ثانية.
- المخرج (LLM) بيكتب **سكربت تقليد**: لقطة لكل لقطة في الأصل — نفس العدد،
  نفس الترتيب، نفس الإيقاع، ونفس نوع الحدث (تحدي؟ إحنا بنعمله. مفاجأة؟ بنعملها).
- كل لقطة ليها استعلام لقطة حقيقي بالإنجليزي (`visual_query`) → المونتاج
  بيجيب لقطة حية جديدة لكل لحظة من بيكساباي/بيكسلز/كومنز — **صفر صور مخزنة**.
- 🚫 ممنوع لافتات «الحقيقة الأولى/التانية» — الحلقة تعيش الحدث، مش تشرحه.
- لو الرؤية/التنزيل وقع: تدهور آمن مع كلام صريح في اللوج (مفيش اختلاق).

## 2) الأرقام من الرادار فقط
- حارس `_assert_radar_numbers`: أي رقم في العنوان/الكلام/نص الشاشة لازم يكون
  مذكور حرفيًا في تقرير الرادار — وإلا الحلقة ترفض وتُعاد كتابتها.

## 3) النشر الساعي — معتمد نهائيًا
- `factory-publish.yml` كل ساعة (`cron '11 * * * *'`) + حارس كل 20 دقيقة
  يضمن إن مفيش فجوة أكبر من **50 دقيقة** (جيت‌هوب بتأخّر/تسقّط المجدول أحيانًا).
- حقيقة جوجل: **6 رفعات/يوم لكل مشروع** (10,000 وحدة ÷ 1,600 للرفعة). المصنع
  بيفضل ينتج كل ساعة، والحلقة الزيادة بتتخزن وتنزل أول ما اليوم يتجدد
  (07:00 UTC = منتصف ليل المحيط الهادي). لتزود السقف: مشروع Google تاني
  (`YOUTUBE_CLIENT_ID_2/_SECRET_2/_REFRESH_TOKEN_2`) → 12/يوم.
- الوصف كامل: إيموجي ودلع ❤️🔔💬 + هاشتاجات + سطور الإسناد CC-BY.

## 4) أمان 100%
- كل المفاتيح والكوكيز من البيئة/GitHub Secrets فقط.
- أي ملف كوكيز مؤقت: صلاحيات 0600 + حذف مضمون في كل الأحوال.
- `.gitignore` فيه `secrets*` و`cookies*` — صفر أسرار في أي ملف متتبع.
